# Beginner Projects

Four separate Python packages:
- List Analyzer
- Calculator
- Emoji Converter
- Rock Paper Scissors

Each has:
- `src/` and `tests/`
- `argparse` CLI
- pytest unit tests
- GitHub Actions + packaging via `pyproject.toml`

